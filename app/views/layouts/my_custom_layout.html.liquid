<!DOCTYPE html>
<html lang="en">
<head>
	{% include 'layout/head' %}
</head>
<body>
  {% include 'layout/header' %}
	<main>
		<div class="container">
			<div class="wrapper">
				<div class="page-title">
					<h1>{% yield 'pagetitle' %}</h1>
					<span class="subtitle">{% yield 'subtitle' %}</span>
				</div>
				<div class="content">
					{{ content_for_layout }}
					
					{% if context.page.metadata.home != 'yes' %}
					{% yield 'back_to_home' %}
					{% endif %}
				</div>
				<div class="tags">
					{% yield 'tags' %}
				</div>
			</div>
		</div>
	</main>
  {% include 'layout/footer' %}
	{% include 'layout/scripts' %}
</body>
</html>
