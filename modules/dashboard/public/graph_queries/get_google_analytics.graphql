query get_google_analytics {
  customizations(is_deleted: false, name: "modules/dashboard/google_analytics") {
    total_entries
    total_pages
    results {
      id
      tracking_id: property(name: "tracking_id")
    }
  }
}
